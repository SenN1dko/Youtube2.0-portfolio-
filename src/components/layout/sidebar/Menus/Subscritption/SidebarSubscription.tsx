import { SubItem } from './SubItem'

export function SidebarSubscription() {
	return (
		<div>
			<ul>
				{/* {menu.map(menuItem => (
					<SubItem
						key={menuItem.label}
						item={menuItem}
					/>
				))} */}
			</ul>
		</div>
	)
}
